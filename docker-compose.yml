version: "3.9"

services:
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama:/root/.ollama

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - ollama

volumes:
  ollama:
